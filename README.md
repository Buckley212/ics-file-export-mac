ics-file-export
============

Generate .ics file in client side.

```
import { ics } from 'ics-file-export';

const cal = ics();

cal.addEvent(
      title,
      description,
      location
      startDate,
      endDate
    );

```


Prior Work / Credit
------------------
* [ICS.js](https://github.com/nwcell):
